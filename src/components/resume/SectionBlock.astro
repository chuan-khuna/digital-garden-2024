---
interface Props {
  title: string
}

const { title } = Astro.props
---

<div class="prose-cactus prose relative max-w-full border p-4 md:block">
  {title && <h1 class="text-2xl">{title}</h1>}
  <slot />
</div>
